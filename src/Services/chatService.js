//var schema = require('../Data/User');

module.exports = chatService =
    function() {
        return {
            Get: Get = function(id) {
                //var user = User.FindByUsername(id);
                return user;

            }
        }
    }